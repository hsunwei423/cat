import Input from 'componentss/common/Input';

const Landing = () => {
  return (
    <div>
      <h1>Search Cat</h1>
      <Input placeholder='input cat name' />
    </div>
  )
};

export default Landing;